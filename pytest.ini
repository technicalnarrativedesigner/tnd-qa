[pytest]
testpaths = tests
pythonpath = .
addopts = --browser chromium --tracing retain-on-failure
base_url = https://www.saucedemo.com
markers =
    smoke: fast critical-path tests
    regression: broader functional coverage
